# Core Harmonia
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=1.0.0
psycopg[binary]>=3.1.0
httpx>=0.25.0
aiohttp>=3.9.0
python-dotenv>=1.0.0
pydantic>=2.0.0
fastapi>=0.100.0
uvicorn>=0.23.0

# Testes
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Comunicação - Telegram
aiohttp>=3.9.0

# Comunicação - Voz (Twilio + NVIDIA)
twilio>=8.10.0

# Opcional: NVIDIA Riva gRPC (para ASR/TTS local)
# grpcio>=1.60.0
# riva-client>=2.13.0  # Instalar via NVIDIA NGC se usar Riva self-hosted

# Opcional: Para processamento de áudio (se precisar manipular WAV/PCM)
# pydub>=0.25.0
# numpy>=1.24.0

# Testes
pytest>=7.4.0
pytest-asyncio>=0.21.0